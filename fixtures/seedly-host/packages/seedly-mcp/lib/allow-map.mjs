/**
 * Fixture stub — SeedlyMCP allow-map so seedly-pin can append pin tools.
 */

/** @type {readonly { operationId: string, name: string }[]} */
export const ALLOW_MAP = [
  { operationId: 'getMe', name: 'get_me' },
  { operationId: 'listContacts', name: 'list_contacts' },
];
