openapi: 3.1.0
info:
  title: Seedly fixture API
  version: 5.8.0
paths:
  /api/v1/contacts:
    get:
      operationId: listContacts
      summary: List contacts
      responses:
        "200":
          description: Contacts

# =============================================================================
# COMPONENTS
components:
  securitySchemes:
    bearerAuth:
      type: http
      scheme: bearer
